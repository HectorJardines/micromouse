#include "../../inc/drivers/adc.h"


void adc_init(void) {
    // configure the peripheral in continuous scan mode
    
    // use dma to continuously xfer data to RAM as we scan
}

void adc_sample_channels(adc_channel_values_t *values) {

}
